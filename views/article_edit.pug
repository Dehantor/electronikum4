extends layout

block content
    br
    br
    h1 #{title}
    form(method='post', action =  '/article/edit/'+article._id)
        #form-group
            input.form-control(type='text',name='title',value = article.title )
        #form-group
            input.form-control(type='text',name='author', value = article.author)
        #form-group
            textarea.form-control(name='body' cols='30', rows = '7')=article.body
        #form-group
            button(type='submit') Обновить
